#include <stdio.h>

int main() {
    int totalMarks = 90;   // valid
    int _count = 5;        // valid
    // int 1value = 10;    // invalid identifier

    printf("%d %d", totalMarks, _count);
    return 0;
}
// This program demonstrates valid and invalid variable identifiers in C.